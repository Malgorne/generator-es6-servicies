# <%= projectName %>

## Overview

<%= projectDescription %>

## Getting Started

Clone the repo:

```
<%= repoType %> clone <%= repoURL %>
cd <%= projectName %>
```

Install yarn:

```
npm install -g yarn
```

To learn to use yarn, see [this page](https://yarnpkg.com/lang/en/docs/usage/).
